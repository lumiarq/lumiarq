/**
 * Welcome Module
 *
 * UI-only module serving the welcome page with no Actions, Queries, or Events
 * Demonstrates how Lumiarq supports static applications.
 *
 * Usage:
 * - Navigate to / to see the welcome page
 * - Content is sourced from content/welcome.md with frontmatter metadata
 * - No Actions, Queries, or Events are exposed since this is a UI-only module
 */

export {}
