import { defineModule } from "@lumiarq/framework"

export default defineModule({
  name: "Welcome",
  alias: "welcome",
  description: "Default landing page — replace with your own index route",
  priority: 0,
})
