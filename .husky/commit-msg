#!/usr/bin/env sh
# Strip Co-authored-by trailers (IDE-injected) so GitHub credits stay human-only.
test -f "$1" || exit 0
tmp="$1.strip-coauthor.$$"
if perl -ne 'print unless /^Co-Authored-By:/i' "$1" >"$tmp" 2>/dev/null; then
  mv "$tmp" "$1"
else
  rm -f "$tmp"
fi
exit 0
